/// Debuggable trait. Canonical definition now lives in core.fmt (same
/// module as Formatter) to avoid a cyclic import — see learning-log L021.
/// This file intentionally declares no items. Prefer
/// `import core.fmt.Debuggable` at call sites.
